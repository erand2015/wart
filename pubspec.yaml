name: warthog_wallet
description: Warthog Wallet

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0  # Kontrollo që ky rresht ekziston këtu
  bip39: ^1.0.6
  bip32: ^2.0.0
  secp256k1: ^0.3.0
  hex: ^0.2.0
  crypto: ^3.0.7
  pointycastle: ^3.7.3
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.1
  http: ^1.2.0
  elliptic: ^0.3.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true